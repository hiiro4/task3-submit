
<div class="container">
    <div class="row">
<%= render"list" %>
<div class="col-md-8 offset-md-1">


<h1>Book detail</h1>
<table class="table">
  <tbody>
 <tr class="s-tr">
  <td class="s-td"><%=image_tag@books.user.get_profile_image(100,100) %></td>
  <td class="s-td"><%=link_to book_path(@books.id) do %><%=@books.title %></td><%end%>
  <td class="s-td"><%=@books.body %></td>
  <% if@books.user==current_user %>
  <td class="s-td"><%=link_to"Edit",edit_book_path(@books)%></td>
　<td class="s-td"><%=link_to"Destroy",book_path(@books),method: :delete,data: {confirm:"Are you sure?"}%></td>
<%end%>
  </tr>
  </tbody>
</table>
 <%= link_to user_path(@book_user) do %> <%=@book_user.name%><%end%>


 </div>
 </div>
  </div>
